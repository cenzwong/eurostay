# eurostay
eurostay is a guide for common eu hotel brand introduction

## Local preview (static site)
1. Start a local server from the repo root:

   ```bash
   python -m http.server 8000
   ```

2. Open `http://localhost:8000/` in your browser to verify the site renders.
